'use client';
import { useEffect, useState } from 'react';
import { RefreshCw, Search } from 'lucide-react';
import { api } from '@/lib/api';
import type { DetectionRule } from '@/types/api';
export default function Rules(){const [rules,setRules]=useState<DetectionRule[]>([]),[q,setQ]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(true); const load=()=>{setLoading(true);api<DetectionRule[]>('/rules?limit=100').then(setRules).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);const filtered=rules.filter(x=>`${x.rule_id} ${x.rule_name} ${x.description||''}`.toLowerCase().includes(q.toLowerCase()));return <main className="page"><div className="eyebrow">DETECTION ENGINE</div><div className="page-header"><div><h1>Detection rules</h1><p>Manage correlation rules and their runtime configuration.</p></div><button className="refresh" onClick={load}><RefreshCw size={14}/> Refresh</button></div>{error&&<div className="api-warning">{error}</div>}<div className="toolbar"><div className="search"><Search size={15}/><input placeholder="Search rules..." value={q} onChange={e=>setQ(e.target.value)}/></div></div><div className="panel table-panel"><div className="table-meta"><span>{loading?'Loading…':`${filtered.length} rules`}</span><span>Role-protected updates</span></div><div className="table-scroll"><table><thead><tr><th>Rule</th><th>Severity</th><th>Status</th><th>Parameters</th><th>Updated</th></tr></thead><tbody>{filtered.map(x=><tr key={x.rule_id}><td><b>{x.rule_name}</b><small>{x.rule_id}<br/>{x.description}</small></td><td><span className={`badge ${x.severity.toLowerCase()}`}>{x.severity}</span></td><td><span className={`status ${x.enabled?'enabled':'disabled'}`}>{x.enabled?'ENABLED':'DISABLED'}</span></td><td className="mono">{Object.keys(x.parameters||{}).length} configured</td><td className="mono">{new Date(x.updated_at).toLocaleString()}</td></tr>)}{!loading&&!filtered.length&&<tr><td colSpan={5}><div className="empty">No rules found.</div></td></tr>}</tbody></table></div></div></main>}
