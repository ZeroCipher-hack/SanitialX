'use client';
import { useEffect, useMemo, useState } from 'react';
import { Search, RefreshCw } from 'lucide-react';
import { api } from '@/lib/api';
import type { Incident } from '@/types/api';
export default function Threats(){const [items,setItems]=useState<Incident[]>([]),[q,setQ]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(true);const load=()=>{setLoading(true);api<Incident[]>('/incidents?limit=100').then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);const threats=useMemo(()=>items.filter(x=>x.severity==='CRITICAL'||x.severity==='HIGH').filter(x=>`${x.title} ${x.source_ip||''} ${x.destination_ip||''}`.toLowerCase().includes(q.toLowerCase())),[items,q]);return <main className="page"><div className="eyebrow">THREAT MANAGEMENT</div><div className="page-header"><div><h1>Threats</h1><p>Centralized view of high-confidence malicious activity.</p></div><button className="refresh" onClick={load}><RefreshCw size={14}/> Refresh</button></div>{error&&<div className="api-warning">{error}</div>}<div className="toolbar"><div className="search"><Search size={15}/><input placeholder="Search threat feed..." value={q} onChange={e=>setQ(e.target.value)}/></div></div><div className="panel table-panel"><div className="table-meta"><span>{loading?'Loading…':`${threats.length} high-priority threats`}</span><span>Derived from incident telemetry</span></div><div className="table-scroll"><table><thead><tr><th>Severity</th><th>Threat</th><th>Source</th><th>Target</th><th>Status</th></tr></thead><tbody>{threats.map(x=><tr key={x.incident_id}><td><span className={`badge ${x.severity.toLowerCase()}`}>{x.severity}</span></td><td><b>{x.title}</b><small>{x.incident_id}</small></td><td className="mono">{x.source_ip||'—'}</td><td className="mono">{x.destination_ip||'—'}</td><td><span className={`status ${x.status.toLowerCase()}`}>{x.status}</span></td></tr>)}{!loading&&!threats.length&&<tr><td colSpan={5}><div className="empty">No high-priority threats found.</div></td></tr>}</tbody></table></div></div></main>}
